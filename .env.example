# Postgres connection string used by Prisma.
# Local example:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/routine_quest?schema=public"

# Optional: change the port the server listens on (defaults to 3000)
# PORT=3000
