# Postgres connection string used by Prisma.
# Locally, run `prisma postgres link` or paste the Prisma Postgres DATABASE_URL here.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Optional: change the port the server listens on (defaults to 3000)
# PORT=3000
